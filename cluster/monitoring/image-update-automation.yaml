---
apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImageUpdateAutomation
metadata:
  name: monitoring
  namespace: monitoring
spec:
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        email: gm908071@gmail.com
        name: gopal
      messageTemplate: '{{range .Updated.Images}}{{println .}}{{end}}'
    push:
      branch: main
  interval: 10m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  update:
    path: ./cluster
    strategy: Setters
